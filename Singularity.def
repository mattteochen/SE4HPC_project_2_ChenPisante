Bootstrap: docker
From: pcafrica/mk #Scientific computing image

%labels
    Maintainer mattteochen
    Version 1.0

%files
    build/ /tmp
    matrixA.txt /tmp
    matrixB.txt /tmp

%post
    apt-get update && apt-get install -y \
    build-essential \
    openmpi-bin \
    openmpi-common \
    cmake \
    libopenmpi-dev

